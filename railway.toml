[build]
builder = "NIXPACKS"
buildCommand = "cd client && npm install && npm run build && cp -r build/* ../server/public/ && cd ../server && npm install"

[deploy]
startCommand = "cd server && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
